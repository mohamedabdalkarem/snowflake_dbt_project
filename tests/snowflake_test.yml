version: 2

models:
  - name: stg_orders
    columns:
      - name: order_status
        tests:
          - accepted_values:
              values: ['Pending', 'Completed', 'Cancelled']

  - name: stg_customers
    columns:
      - name: customer_name
        tests:
          - unique
